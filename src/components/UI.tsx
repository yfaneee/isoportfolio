import React from 'react';
import './UI.css';

const UI: React.FC = () => {
  return (
    <div className="ui-overlay">
      {/* Clean UI - no navigation, instructions, or footer */}
    </div>
  );
};

export default UI;
